// Keys that must be included in the search params
export const USERS_SEARCH_PARAMS_KEYS = Object.freeze({
  input: 'search', // string
  limit: 'limit', // number
});

// Number of items to fetch
export const USERS_COUNT: number = 5; // default count
export const USERS_MAX_LIMIT: number = 10;
